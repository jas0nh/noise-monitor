{
  "name": "noise-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:capture": "mkdir -p build/NoiseCapture.app/Contents/MacOS && cp app/NoiseCapture/Info.plist build/NoiseCapture.app/Contents/Info.plist && swiftc scripts/noise-capture.swift -o build/NoiseCapture.app/Contents/MacOS/NoiseCapture -framework AVFoundation && codesign --force --sign - build/NoiseCapture.app",
    "sample": "node scripts/noise-sampler.mjs"
  },
  "engines": {
    "node": ">=22.5"
  }
}
